#include "benchmark.h"
#include "../src/sorting/sorting.h"

int main(void) {
    // TODO: Benchmark each sort on random, sorted, reversed, and duplicate-heavy input.
    // Rebuild identical caller-owned pointer arrays before every timed sample.
    return 0;
}
