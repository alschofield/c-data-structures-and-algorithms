#include "benchmark.h"
#include "../src/search/search.h"

int main(void) {
    // TODO: Benchmark linear and binary lookup at increasing sorted input sizes.
    // Keep data setup, result verification, and teardown outside timed batches.
    return 0;
}
